[project]
name = "compile-flashbot"
authors = ["Your Name"]
description = "Automated computational task tracking and reward platform"
telemetry = false
requirements = []
[contracts.flashbot-tracker]
path = "contracts/flashbot-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
